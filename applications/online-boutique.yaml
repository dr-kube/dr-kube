# ArgoCD Application: Online Boutique
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: online-boutique
  namespace: argocd
  # finalizers 제거 시 ArgoCD가 헬스체크 건너뜀
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    # 로컬 차트 사용 (원격 GoogleCloudPlatform 차트 대체)
    # templates에 replicas/env/probe/HPA/PDB 지원 추가
    - repoURL: https://github.com/dr-kube/dr-kube.git
      targetRevision: main
      path: helm-chart/online-boutique
      helm:
        valueFiles:
          - $values/values/online-boutique.yaml

    - repoURL: https://github.com/dr-kube/dr-kube.git
      targetRevision: main
      ref: values

    - repoURL: https://github.com/dr-kube/dr-kube.git
      targetRevision: main
      path: manifests/online-boutique

  destination:
    server: https://kubernetes.default.svc
    namespace: online-boutique

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

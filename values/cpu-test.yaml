# DR-Kube CPU Throttle 테스트용 애플리케이션
# CPU limit을 낮게 설정하여 throttling 유발 -> 이슈 해결을 위해 리소스 할당량 상향 조정

app:
  name: cpu-test
  namespace: default

  replicas: 1

  container:
    name: cpu-stress
    image: polinux/stress
    tag: latest

    command:
      - "stress"

    resources:
      requests:
        memory: "64Mi"
        cpu: "1000m"  # 워커 부하를 고려하여 요청량 상향 조정
      limits:
        memory: "256Mi"
        cpu: "2000m"  # 2개의 CPU 워커(args: --cpu 2) 부하를 충분히 수용할 수 있도록 2000m로 상향

    args:
      - "--cpu"
      - "2"         # CPU 워커 2개 (2000m limit에 부합)
      - "--timeout"
      - "3600"      # 1시간 실행

  labels:
    scenario: cpu-throttle
    managed-by: dr-kube
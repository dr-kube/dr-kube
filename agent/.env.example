# LLM 프로바이더 설정
# [ollama] (기본값) or [gemini]
LLM_PROVIDER=gemini

# Ollama 설정 (LLM_PROVIDER=ollama일 때)
OLLAMA_MODEL=qwen2.5:14b

# Gemini 설정 (LLM_PROVIDER=gemini일 때)
GEMINI_API_KEY=your-api-key-here
GEMINI_MODEL=gemini-2.5-flash

# 웹훅 서버 설정
WEBHOOK_HOST=0.0.0.0
WEBHOOK_PORT=8080
AUTO_PR=false
